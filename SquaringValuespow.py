def square(b):
  c = pow(b,2)
  return c


a = int(input("Enter the vaules: "))
b = square(a)
print(b)

Note:
  pow() function is used to square the values
  Example: if pow(5,2) where is the number we need to square
            specified 2 to square
